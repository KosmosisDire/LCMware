"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""


from io import BytesIO
import struct

import core

class ProcessDataGoal(object):
    """ Data processing action """

    __slots__ = ["header", "operation", "data_size", "data"]

    __typenames__ = ["core.Header", "string", "int32_t", "int8_t"]

    __dimensions__ = [None, None, None, ["data_size"]]

    def __init__(self):
        self.header = core.Header()
        """ LCM Type: core.Header """
        self.operation = ""
        """ LCM Type: string """
        self.data_size = 0
        """
        "sort", "filter", "transform", etc.
        LCM Type: int32_t
        """

        self.data = []
        """ LCM Type: int8_t[data_size] """

    def encode(self):
        buf = BytesIO()
        buf.write(ProcessDataGoal._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        assert self.header._get_packed_fingerprint() == core.Header._get_packed_fingerprint()
        self.header._encode_one(buf)
        __operation_encoded = self.operation.encode('utf-8')
        buf.write(struct.pack('>I', len(__operation_encoded)+1))
        buf.write(__operation_encoded)
        buf.write(b"\0")
        buf.write(struct.pack(">i", self.data_size))
        buf.write(struct.pack('>%db' % self.data_size, *self.data[:self.data_size]))

    @staticmethod
    def decode(data: bytes):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != ProcessDataGoal._get_packed_fingerprint():
            raise ValueError("Decode error")
        return ProcessDataGoal._decode_one(buf)

    @staticmethod
    def _decode_one(buf):
        self = ProcessDataGoal()
        self.header = core.Header._decode_one(buf)
        __operation_len = struct.unpack('>I', buf.read(4))[0]
        self.operation = buf.read(__operation_len)[:-1].decode('utf-8', 'replace')
        self.data_size = struct.unpack(">i", buf.read(4))[0]
        self.data = struct.unpack('>%db' % self.data_size, buf.read(self.data_size))
        return self

    @staticmethod
    def _get_hash_recursive(parents):
        if ProcessDataGoal in parents: return 0
        newparents = parents + [ProcessDataGoal]
        tmphash = (0x69656acfa7cb3a9d+ core.Header._get_hash_recursive(newparents)) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff) + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _packed_fingerprint = None

    @staticmethod
    def _get_packed_fingerprint():
        if ProcessDataGoal._packed_fingerprint is None:
            ProcessDataGoal._packed_fingerprint = struct.pack(">Q", ProcessDataGoal._get_hash_recursive([]))
        return ProcessDataGoal._packed_fingerprint

    def get_hash(self):
        """Get the LCM hash of the struct"""
        return struct.unpack(">Q", ProcessDataGoal._get_packed_fingerprint())[0]

